# =============================================================================
# SLAM Toolbox Parameters — Samurai Robot
# Online async mode, using camera-derived pseudo-scan + ultrasonic range
# =============================================================================

slam_toolbox:
  ros__parameters:
    # General
    odom_frame: odom
    map_frame: map
    base_frame: base_link
    scan_topic: /scan            # MiDaS monocular depth → pseudo-scan
    use_scan_matching: true
    use_scan_barycenter: true
    mode: mapping                # 'mapping' or 'localization'

    # Update thresholds — tighter for narrow FOV camera (~62°)
    minimum_travel_distance: 0.03
    minimum_travel_heading: 0.05  # radians (~3°), smaller steps = more scans
    scan_buffer_size: 15          # more scans in buffer for better matching
    scan_buffer_maximum_scan_distance: 3.0

    # Map properties
    resolution: 0.05
    max_laser_range: 3.0
    minimum_time_interval: 0.3    # accept scans more frequently
    transform_timeout: 0.5        # more tolerance for WiFi DDS latency
    tf_buffer_duration: 30.0

    # Solver
    solver_plugin: solver_plugins::CeresSolver
    ceres_linear_solver: SPARSE_NORMAL_CHOLESKY
    ceres_preconditioner: SCHUR_JACOBI
    ceres_trust_strategy: LEVENBERG_MARQUARDT
    ceres_dogleg_type: TRADITIONAL_DOGLEG
    ceres_loss_function: None

    # Loop closure — relaxed thresholds for narrow FOV
    enable_interactive_mode: false
    loop_search_maximum_distance: 3.0
    do_loop_closing: true
    loop_match_minimum_chain_size: 6     # fewer scans needed to form a chain
    loop_match_maximum_variance_coarse: 5.0   # more tolerance
    loop_match_minimum_response_coarse: 0.25  # lower bar for coarse match
    loop_match_minimum_response_fine: 0.35    # lower bar for fine match

    # Correlation search — wider search for narrow FOV
    correlation_search_space_dimension: 0.75
    correlation_search_space_resolution: 0.01
    correlation_search_space_smear_deviation: 0.15

    # Loop closure search
    loop_search_space_dimension: 8.0
    loop_search_space_resolution: 0.05
    loop_search_space_smear_deviation: 0.03

    # Other
    distance_variance_penalty: 0.5
    angle_variance_penalty: 1.0
    fine_search_angle_offset: 0.00349
    coarse_search_angle_offset: 0.349
    coarse_angle_resolution: 0.0349
    minimum_angle_penalty: 0.9
    minimum_distance_penalty: 0.5
    use_response_expansion: true

    # Map serialization
    map_file_name: ""
    map_start_pose: [0.0, 0.0, 0.0]
    map_start_at_dock: true

    debug_logging: false
    throttle_scans: 1
    transform_publish_period: 0.02
    map_update_interval: 5.0

    use_sim_time: false
    stack_size_to_use: 40000000
